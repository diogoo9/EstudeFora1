<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.estudefora">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Tour2Activity" />
        <activity android:name=".Tour3Activity" />
        <activity android:name=".Tour4Activity" />
        <activity android:name=".Tour5Activity" />
        <activity android:name=".Tour6Activity" />
        <activity android:name=".MoreInfoActivity" />
        <activity android:name=".TourActivity" />
        <activity android:name=".Tour0Activity" />
        <activity android:name=".Tour01Activity" />
        <activity android:name=".ComousarActivity"></activity>
    </application>

</manifest>
